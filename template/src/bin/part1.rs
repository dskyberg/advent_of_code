use anyhow::Result;

use aoc_utils::*;
use {{crate_name}}::*;

fn main() -> Result<()> {
    let  result = 0;
    let mut aoc = Aoc::part1();

    // Placeholder to kill warning
    println!("{}", INPUT);

    aoc.result(result);
    println!("{}", aoc);
    Ok(())
}
